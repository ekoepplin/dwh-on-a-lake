[project]
name = "dwh-in-a-box"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11.11,<3.12.0"

dependencies = [
    "datetime>=5.5",
    "dbt-core>=1.8.0,<2.0.0",
    "dbt-duckdb>=1.8.0,<2.0.0",
    "dlt[bigquery,duckdb,workspace]>=1.16.0",
    "duckdb>=1.4.2,<2.0.0",
    "loguru>=0.7.3",
    "newsapi==0.1.1",
    "newsapi-python==0.2.7",
    "requests>=2.32.5",
    "typing_extensions>=4.5.0",  # Required for pydantic_core (Sentinel support)
    "pyyaml>=6.0",  # Added for YAML parsing in validators
]