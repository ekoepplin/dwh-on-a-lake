dwh-in-a-box-profile:
  outputs:
    dev:
      type: duckdb
      extensions:
        - httpfs
        - parquet
        - aws
      path: ../reports/sources/duckdb/newsapi_articles.duckdb
      schema: ingest_newsapi_v1
      threads: 1
  target: dev